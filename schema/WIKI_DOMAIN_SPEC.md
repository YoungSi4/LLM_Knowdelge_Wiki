# 지식 도메인 정의서 (Domain Definition)

## 1. 도메인 개요
본 Wiki Tool이 다루는 핵심 지식 도메인은 **게임 개발(Game Development)에서의 데이터 지향 설계(Data-Oriented Design, DOD)** 입니다. 
기존의 객체 지향 프로그래밍(OOP)이 야기하는 메모리 병목 현상을 극복하고, 하드웨어 친화적인 아키텍처를 통해 게임의 성능을 극대화하는 패러다임을 중점적으로 다룹니다.

## 2. 타겟 독자 (Target Audience)
- **메인 타겟:** 본인(작성자)과 같은 현업/독학 게임 개발자.
- **활용 목적:** 학습한 DOD 패러다임과 최적화 기법을 체계적으로 축적(Archive)하고, 개발 중 빠르게 열람(Retrieval) 및 적용하기 위한 개인 지식 베이스(Second Brain).

## 3. 구축 목적 (Purpose & Goal)
1. **성능 병목 해결:** 복잡한 게임 로직 연산에서 발생하는 CPU 및 메모리 병목 현상의 원인을 분석하고, 이를 해결하기 위한 최적화 기법을 기록합니다.
2. **패러다임의 체계적 학습:** 진입 장벽이 높고 다루기 까다로운 데이터 지향 설계(DOD)의 파편화된 지식들을 하나의 위키 구조로 통합하여, 필요할 때 AI 에이전트를 통해 쉽게 찾고 학습할 수 있는 환경을 구축합니다.

## 4. 핵심 하위 주제 (Core Sub-topics)
위키에 주로 수록될 세부 지식 카테고리는 다음과 같습니다.
- **ECS (Entity Component System) 아키텍처:** 데이터와 로직의 분리, 시스템 기반의 병렬 처리 기법.
- **CPU 캐시 지역성 (Cache Locality):** L1/L2/L3 캐시 히트율을 높이기 위한 데이터 배치 전략.
- **메모리 레이아웃 (AoS vs SoA):** Array of Structures와 Structure of Arrays의 차이 및 성능 비교.
- **멀티스레딩 최적화:** DOD 구조 하에서의 동시성(Concurrency) 제어 및 Job System 활용.
- **DOD 기반 물리 엔진 (Physics Engine):** 방대한 오브젝트 충돌 및 연산을 데이터 지향적으로 처리하는 물리 엔진 아키텍처 설계 기법.
